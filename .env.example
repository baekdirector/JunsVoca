# Copy to .env for local development (never commit the real file).
# Render sets this directly as an environment variable in the dashboard.
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
PORT=3000
